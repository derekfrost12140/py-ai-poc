{
  "tools": [
    {
      "name": "weather_tool",
      "description": "Get current weather information for a specific location",
      "type": "rest",
      "parameters": {
        "location": {
          "type": "string",
          "description": "City name or location to get weather for",
          "required": true
        }
      }
    },
    {
      "name": "sql_tool",
      "description": "Execute SQL queries on the local database",
      "type": "sql",
      "parameters": {
        "sql_query": {
          "type": "string",
          "description": "SQL query to execute",
          "required": true
        }
      }
    },
    {
      "name": "graphql_tool",
      "description": "Query SpaceX GraphQL API for mission and launch information",
      "type": "graphql",
      "parameters": {
        "query": {
          "type": "string",
          "description": "GraphQL query to execute",
          "required": true
        }
      }
    },
    {
      "name": "system_info_tool",
      "description": "Get information about the system architecture, available tools, and capabilities",
      "type": "system",
      "parameters": {
        "query": {
          "type": "string",
          "description": "System-related question about tools, architecture, or capabilities",
          "required": true
        }
      }
    }
  ]
} 